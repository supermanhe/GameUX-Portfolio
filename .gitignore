node_modules
.next
.env*
*.local
dist
*.log
out

# build artifacts
*.tsbuildinfo

# local-only Claude Code settings (keep launch.json shared)
.claude/settings.local.json

# raw slice working material (unreferenced; assets served from Cloudinary)
.dragon-slices/

